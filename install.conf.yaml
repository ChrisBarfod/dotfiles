- link:
      # create: true
      # path: config/terminator
     # ~/.vim: vim
    # ~/.vimrc:
      # relink: true
      # path: vimrc
    ~/.config/nvim/init.vim: init.vim
    ~/.zshrc: zshrc
    ~/.gitconfig: gitconfig
